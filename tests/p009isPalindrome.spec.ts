import { expect } from "chai";
import { isPalindrome } from "../src/p009isPalindrome";

describe("P001 sum", () => {
  it("test1", () => {
    console.log("Starting Test For Test One P001 SUM");
    expect(isPalindrome(-201)).deep.equals(false);
  });
});
